'''
    此程序用来操作元组
    元组:Python将不能修改的值称为不可变的，而不可变的列表被称为元组



'''
#元组看起来犹如列表，但使用圆括号而不是方括号来标识
#定义一个元组
dimensions=(200,50)
print(dimensions[0])
print(dimensions[1])
#dimensions[0]=1  错误 不能对tuple(元组)类型操作

